// src/api/config.js
// Central place for the MockAPI base URL — every screen imports from here
// instead of hardcoding the link, so if the API ID ever changes we only
// update it once.
export const API_URL = 'https://6a9ed8932f89be7fb70ea790.mockapi.io/exercises';